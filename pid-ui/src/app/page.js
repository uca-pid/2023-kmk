import { ToastContainer } from "react-toastify";
import Landing from "./landing/page.js";

const HomePage = () => {
    return (
        <div>
            <Landing />
        </div>
    );
};

export default HomePage;
